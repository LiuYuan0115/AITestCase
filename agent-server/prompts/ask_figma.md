# Figma → 测试友好交互补充（Figma for QA）

## 触发条件
用户提供 Figma URL（包含 /file/ 或 /design/）。

## 你的任务
基于 Figma 数据生成一份"可直接用于补充PRD与生成测试用例"的文档：
重点是交互、状态、规则、可测断言点；不要输出大段视觉描述（颜色/字号/间距等）。

## 输入
- Figma URL：{url}
- get_figma_data 工具返回的结构化数据

## 输出格式（必须是 Markdown，不要代码块）
# {需求名称（来自Figma文件/页面名）}

## 来源
- Figma 文件：{文件名}
- URL：{原始URL}
- 解析时间：{时间戳}

## 页面/组件清单（测试视角）
- 页面：{Page A}
  - 区块：{Section}
  - 关键组件：{Component list}

## 交互与状态（最重要）
按页面/区块输出：
### {页面/区块名称}
#### 可见状态
- 默认态：
- Loading态：
- 空态：
- 错误态：
- 禁用态/权限态（如有）：

#### 用户操作与响应
- 点击 {按钮/入口} → 触发 {行为} → 页面变化 {跳转/弹窗/刷新/滚动/埋点} → 失败提示 {口径}
- 输入 {字段}：规则 {类型/长度/必填/格式/去空格/非法字符}，校验时机 {输入中/失焦/提交}，错误提示 {文案或占位}

#### 列表/分页/筛选（如有）
- 排序规则：
- 筛选项：
- 翻页/加载更多：

## 测试钩子建议（强烈建议）
- 为关键元素建议 data-testid（或 data-ext-id）命名：
  - 页面：{page}-root
  - 主CTA：{page}-cta-primary
  - 表单字段：{page}-field-{name}
  - 错误提示：{page}-error-{name}
  - 弹窗：{page}-dialog-{name}

## 可直接转测试点的断言清单
- UI断言：
- 接口断言（如能从设计推断到请求/提交）：
- 数据断言（如有状态变更）：

## 约束
- 不要推导新需求，不要猜测后端规则；缺失则写"待PRD确认"
- 不要输出大量颜色/间距/字体细节，除非它直接影响交互或可用性（如禁用态仅靠颜色区分）
